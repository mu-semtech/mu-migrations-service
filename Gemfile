# frozen_string_literal: true

source "https://rubygems.org"

git_source(:github) {|repo_name| "https://github.com/#{repo_name}" }

gem "mu-auth-sudo", github: 'redpencilio/mu-auth-sudo-ruby.git'
gem "rdf-turtle", "~> 3.0"
